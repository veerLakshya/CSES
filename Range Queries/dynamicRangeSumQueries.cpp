#include<bits/stdc++.h>
using namespace std;

int main()
{
    int n, q;
    cin >> n >> q;
    vector<int> arr(n + 1, 0);
    for (int i = 1; i <= n; i++) cin >> arr[i];
    vector<pair<int, int>> queries;
    for (int i = 0;i < q;i++)
    {

    }



    for (int i = 0;i < n;i++)
        cout << arr[i];

    return 0;
}
